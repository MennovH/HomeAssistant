#include <tunables/global>

profile cloudflareddns flags=(attach_disconnected,mediate_deleted,complain) {
  #include <abstractions/base>

  # Capabilities
  file,
  signal (send) set=(kill,term,int,hup,cont),

  network inet tcp,
  network inet udp,

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /package/** ix,
  /command/** ix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Access to options.json and other files within your addon
  /data/** rw,

  # Access required for service functionality
  /usr/bin/cloudflareddns r,
  /bin/wget/** w,
  /bin/curl/** w,
  /bin/bash rix,
  /bin/echo ix,
  /dev/tty rw,
}
